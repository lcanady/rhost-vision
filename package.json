{
  "name": "@rhost/vision",
  "version": "0.1.0",
  "description": "Run a command against a RhostMUSH server (Docker or existing)",
  "bin": {
    "mush-vision": "./bin/mush-run.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [
    "mush",
    "rhostmush",
    "rhost",
    "mushcode",
    "cli"
  ],
  "license": "MIT",
  "dependencies": {
    "@rhost/testkit": "^1.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "vitest": "^4.1.2"
  }
}
